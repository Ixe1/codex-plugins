interface:
  display_name: "Maquette Direction"
  short_description: "Create greenfield page direction concepts before the brand kit is locked."
  default_prompt: "Use $maquette-direction to explore greenfield website direction before creating a brand kit. Create a lightweight direction brief, resolve Maquette image-worker authorization before any image_gen call, generate and inspect concrete page direction concepts, ask which direction to use unless the user explicitly requested unattended/no-question mode, then write a direction inventory that records visual direction, hierarchy, component needs, asset needs, responsive implications, accessibility risks, ambiguities, and page-local flourishes for the brand-kit and component phases."
