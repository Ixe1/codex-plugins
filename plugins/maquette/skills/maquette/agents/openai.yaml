interface:
  display_name: "Maquette"
  short_description: "Run the full staged image-guided website workflow."
  default_prompt: "Use $maquette to run the full Maquette workflow: create any missing focused foundational brand kit, then process component coverage sequentially by checking optional QA tooling, generating one focused 16:9 sheet at a time, and writing concrete category-prefixed componentized batch replica/reference, CSS/JS, catalog, and screenshot-reviewed artifacts before moving to the next needed sheet. Then create the requested page or screen with concept-region inventory and fidelity QA."
