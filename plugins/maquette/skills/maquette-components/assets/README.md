Use `shared/component-contract.schema.json` and `shared/component-catalog.schema.json` together with the approved design system and executable brand canon.

The coded componentized reference should be reviewed as a faithful browser implementation of the structured component contract and brand proof, including component families, variants, states, repeated-card anatomy, responsive navigation, newsletter modules, and footer/social modules.

Prompt assets:

- `component-css-contract-prompt.md`: legacy fallback prompt for explicit image-generated CSS-poster requests.
- `component-sheet-prompt.md`: visual component sheet prompt for explicit visual-sheet requests only.
