# Component Sheet Inventory Template

Use this for `.maquette/components/sheet-inventory.md`.

## Sheet

- Batch:
- Visual sheet path:
- Inspected artifact path:
- Deterministic contract path:
- Status:

## Visible Regions

| Region ID | Sheet location | Kind | Visible content | States/variants shown | Responsive variants shown | Required selectors/slots | Implementation status | Evidence | Omission or simplification reason |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| example-region | upper-left | component | Primary, secondary, disabled button strip | default, hover, focus-visible, disabled | none | `.btn`, `.btn--primary`; slots: label, icon-start | implemented | `components/core-primitives.replica.html` | none |

Implementation status must be one of: `implemented`, `partial`, `simplified`, `omitted`, `approved-omission`, `needs-regeneration`, or `blocked`.

## Gate

- Visible regions listed:
- Visible regions implemented:
- State variants represented:
- Desktop/tablet/mobile variants represented when shown:
- No unapproved simplification:
- Approved/documented omissions:
